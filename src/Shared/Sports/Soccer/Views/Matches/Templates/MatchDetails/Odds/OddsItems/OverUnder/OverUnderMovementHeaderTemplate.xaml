<?xml version="1.0" encoding="UTF-8" ?>
<DataTemplate x:Class="LiveScore.Soccer.Views.Matches.Templates.MatchDetails.Odds.OddsItems.OverUnder.OverUnderMovementHeaderTemplate"
              xmlns="http://xamarin.com/schemas/2014/forms"
              xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:languageResources="clr-namespace:LiveScore.Common.LangResources;assembly=LiveScore.Common"
              xmlns:resources="clr-namespace:LiveScore.Soccer.ViewResources">
    <ViewCell Height="{StaticResource TableRowHeight}">
        <StackLayout>
            <StackLayout.Resources>
                <ResourceDictionary>
                    <ResourceDictionary.MergedDictionaries>
                        <resources:MatchDetailViewResource />
                        <resources:MatchDetailOddsViewResource />
                    </ResourceDictionary.MergedDictionaries>
                </ResourceDictionary>
            </StackLayout.Resources>
            <BoxView Style="{StaticResource TableLine}" />
            <Grid Style="{StaticResource GridTableContainer}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.25*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="1.75*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="{StaticResource TableRowHeight}" />
                </Grid.RowDefinitions>
                <Label Grid.Row="0"
                       Grid.Column="0"
                       Style="{StaticResource TableHeaderText}"
                       Text="{languageResources:Translate OddsHeaderTime}" />
                <Label Grid.Row="0"
                       Grid.Column="1"
                       Style="{StaticResource TableHeaderText}"
                       Text="{languageResources:Translate OddsHeaderScore}" />
                <Label Grid.Row="0"
                       Grid.Column="2"
                       Style="{StaticResource TableHeaderText}"
                       Text="{languageResources:Translate Over}" />
                <Label Grid.Row="0"
                       Grid.Column="3"
                       Style="{StaticResource TableHeaderText}"
                       Text="O/U" />
                <Label Grid.Row="0"
                       Grid.Column="4"
                       Style="{StaticResource TableHeaderText}"
                       Text="{languageResources:Translate Under}" />
                <Label Grid.Row="0"
                       Grid.Column="5"
                       Style="{StaticResource TableHeaderText}"
                       Text="{languageResources:Translate OddsHeaderLastUpdated}" />
            </Grid>
        </StackLayout>
    </ViewCell>
</DataTemplate>