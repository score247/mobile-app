<?xml version="1.0" encoding="UTF-8"?>
<DataTemplate xmlns="http://xamarin.com/schemas/2014/forms"
              xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:d="http://xamarin.com/schemas/2014/forms/design"
              xmlns:resources="clr-namespace:LiveScore.Soccer.ViewResources"
              mc:Ignorable="d"
              xmlns:viewModels="clr-namespace:LiveScore.Soccer.ViewModels.Matches.MatchDetails.LineUps"
              xmlns:lineUps="clr-namespace:LiveScore.Soccer.Views.Matches.Templates.MatchDetails.LineUps;assembly=LiveScore.Soccer"
              x:DataType="viewModels:LineupsPlayerViewModel"
              x:Class="LiveScore.Soccer.Views.Matches.Templates.MatchDetails.LineUps.LineupsPlayerTemplate">
    <ViewCell>
        <Grid CompressedLayout.IsHeadless="true">
            <Grid.Resources>
                <ResourceDictionary>
                    <ResourceDictionary.MergedDictionaries>
                        <resources:MatchDetailViewResource />
                        <resources:MatchDetailLinesUpViewResource />
                    </ResourceDictionary.MergedDictionaries>
                </ResourceDictionary>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{StaticResource LineupIconWidthSize}" />
                <ColumnDefinition Width="{StaticResource SubstitutionShirtNumberWidthSize}" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="{StaticResource LineupIconWidthSize}" />
                <ColumnDefinition Width="{StaticResource SubstitutionShirtNumberWidthSize}" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="1" />
            </Grid.RowDefinitions>
            <lineUps:LineupsPlayerStatisticTemplate Grid.Column="0"
                                                    Grid.Row="0"
                                                    PlayerStatistics="{Binding HonePlayerStatistics}" />
            <Label Grid.Column="1"
                   Grid.Row="0"
                   Text="{Binding HomeJerseyNumber}"
                   Style="{StaticResource MatchDetailLinesUpHomeShirtNumber}" />
            <Label Grid.Column="2"
                   Grid.Row="0"
                   Text="{Binding HomeName}"
                   Style="{StaticResource MatchDetailLinesUpSubstitutePlayer}" />

            <lineUps:LineupsPlayerStatisticTemplate Grid.Column="3"
                                                    PlayerStatistics="{Binding AwayPlayerStatistics}" />
            <Label Grid.Column="4"
                   Grid.Row="0"
                   Text="{Binding AwayJerseyNumber}"
                   Style="{StaticResource MatchDetailLinesUpAwayShirtNumber}" />
            <Label Grid.Column="5"
                   Grid.Row="0"
                   Text="{Binding AwayName}"
                   Style="{StaticResource MatchDetailLinesUpSubstitutePlayer}" />
            <BoxView  Grid.Row="1"
                      Grid.ColumnSpan="6"
                      Style="{StaticResource HorizontalLine}" />
        </Grid>
    </ViewCell>
</DataTemplate>