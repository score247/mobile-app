<?xml version="1.0" encoding="UTF-8" ?>
<DataTemplate
        x:Class="LiveScore.Soccer.Views.Templates.MatchDetails.DetailOdds.OddsItems.OverUnder.OverUnderMovementHeaderTemplate"
              xmlns="http://xamarin.com/schemas/2014/forms"
              xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:resources="clr-namespace:LiveScore.Soccer.ViewResources">
    <ViewCell Height="{StaticResource TableRowHeight}">
        <StackLayout>
            <StackLayout.Resources>
                <ResourceDictionary>
                    <ResourceDictionary.MergedDictionaries>
                        <resources:MatchDetailViewResource />
                        <resources:MatchDetailOddsViewResource />
                    </ResourceDictionary.MergedDictionaries>
                </ResourceDictionary>
            </StackLayout.Resources>
            <BoxView Style="{StaticResource TableLine}" />
            <Grid Style="{StaticResource GridTableContainer}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.25*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="1.75*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="{StaticResource TableRowHeight}" />
                </Grid.RowDefinitions>
                <Label Grid.Row="0"
                       Grid.Column="0"
                       Style="{StaticResource TableHeaderText}"
                       Text="TIME" />
                <!--TODO: Add language-->
                <Label Grid.Row="0"
                       Grid.Column="1"
                       Style="{StaticResource TableHeaderText}"
                       Text="SCORE" />
                <Label Grid.Row="0"
                       Grid.Column="2"
                       Style="{StaticResource TableHeaderText}"
                       Text="OVER" />
                <Label Grid.Row="0"
                       Grid.Column="3"
                       Style="{StaticResource TableHeaderText}"
                       Text="O/U" />
                <Label Grid.Row="0"
                       Grid.Column="4"
                       Style="{StaticResource TableHeaderText}"
                       Text="UNDER" />
                <Label Grid.Row="0"
                       Grid.Column="5"
                       Style="{StaticResource TableHeaderText}"
                       Text="LAST UPDATED" />
            </Grid>
        </StackLayout>
    </ViewCell>
</DataTemplate>