<?xml version="1.0" encoding="UTF-8"?>
<DataTemplate x:Class="LiveScore.Soccer.Views.Templates.DetailOdds.OddsTemplate" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms" xmlns:coreConverters="clr-namespace:LiveScore.Core.Converters;assembly=LiveScore.Core" xmlns:d="http://xamarin.com/schemas/2014/forms/design" xmlns:langs="clr-namespace:LiveScore.Common.LangResources;assembly=LiveScore.Common" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:resources="clr-namespace:LiveScore.Soccer.ViewResources" xmlns:tabStrip="clr-namespace:LiveScore.Core.Controls.TabStrip;assembly=LiveScore.Core"
    xmlns:templateSelectors="clr-namespace:LiveScore.Soccer.Views.Templates.DetailOdds" mc:Ignorable="d">
    <StackLayout Padding="0,8">
        <StackLayout.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <resources:MatchDetailViewResource />
                    <resources:MatchDetailOddsViewResource />
                </ResourceDictionary.MergedDictionaries>
                <templateSelectors:OddsItemTemplateSelector x:Key="oddsItemTemplateSelector" />
                <coreConverters:SelectedTabColorConverter x:Key="selectedTabColorConverter" />
                <coreConverters:SelectedTabBackgroundColorConverter x:Key="selectedTabBgColorConverter" />
            </ResourceDictionary>
        </StackLayout.Resources>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="256" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <FlexLayout Grid.Row="0" Grid.Column="0" Style="{StaticResource SubTabBox}">
                <Button BackgroundColor="{Binding IsOneXTwoSelected, Converter={StaticResource selectedTabBgColorConverter}}}" Command="{Binding OnOddsTabClicked}" CommandParameter="1" Style="{StaticResource SubTab}" Text="{langs:Translate OneXTwo}" TextColor="{Binding IsOneXTwoSelected, Converter={StaticResource selectedTabColorConverter}}}" />
                <BoxView Style="{StaticResource SubTabLine}" />
                <Button BackgroundColor="{Binding IsAsianHdpSelected, Converter={StaticResource selectedTabBgColorConverter}}}" Command="{Binding OnOddsTabClicked}" CommandParameter="3" Style="{StaticResource SubTab}" Text="{langs:Translate AsianHDP}" TextColor="{Binding IsAsianHdpSelected, Converter={StaticResource selectedTabColorConverter}}}" />
                <BoxView Style="{StaticResource SubTabLine}" />
                <Button BackgroundColor="{Binding IsOverUnderSelected, Converter={StaticResource selectedTabBgColorConverter}}}" Command="{Binding OnOddsTabClicked}" CommandParameter="2" Style="{StaticResource SubTab}" Text="{langs:Translate OverUnder}" TextColor="{Binding IsOverUnderSelected, Converter={StaticResource selectedTabColorConverter}}}" />
            </FlexLayout>
            <StackLayout Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Style="{StaticResource MatchDetailOddsDropdownContainer}">
                <Label Style="{StaticResource MatchDetailOddsDropdownText}" Text="{langs:Translate OddsTypeDecimal}" />
                <Image Style="{StaticResource DropdownIcon}" />
            </StackLayout>
        </Grid>
        <BoxView Style="{StaticResource SubTabHorizontalLine}" />
        <ActivityIndicator Margin="20" IsEnabled="{Binding IsLoading}" IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" Style="{StaticResource CenterActivityIndicator}" VerticalOptions="StartAndExpand" />
        <StackLayout Style="{StaticResource TableContainer}">
            <tabStrip:TabITemListView AutomationId="{Binding SelectedBetType, StringFormat='MatchOdds-BetType{0}'}" AutomationProperties.IsInAccessibleTree="False" BackgroundColor="{StaticResource PrimaryColor}" HasUnevenRows="true" Header="{Binding}" HeaderTemplate="{Binding HeaderTemplate}" IsPullToRefreshEnabled="true" IsRefreshing="{Binding IsRefreshing}" IsVisible="{Binding IsNotLoading}" ItemTemplate="{StaticResource oddsItemTemplateSelector}"
                ItemsSource="{Binding BetTypeOdds}" MinimumHeightRequest="1000" RefreshCommand="{Binding RefreshCommand}" SeparatorVisibility="None">
                <tabStrip:TabITemListView.Behaviors>
                    <b:EventToCommandBehavior Command="{Binding TappedOddsItemCommand}" EventArgsParameterPath="Item" EventName="ItemTapped" />
                </tabStrip:TabITemListView.Behaviors>
            </tabStrip:TabITemListView>
        </StackLayout>
    </StackLayout>
</DataTemplate>