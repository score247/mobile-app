<?xml version="1.0" encoding="UTF-8" ?>
<DataTemplate
    x:Class="LiveScore.Soccer.Views.Templates.MatchDetailInfo.DefaultItemTemplate"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:resources="clr-namespace:LiveScore.Soccer.ViewResources">
    <ViewCell>
        <StackLayout>
            <StackLayout.Resources>
                <ResourceDictionary>
                    <ResourceDictionary.MergedDictionaries>
                        <resources:MatchDetailViewResource />
                        <resources:MatchDetailInfoViewResource />
                    </ResourceDictionary.MergedDictionaries>
                </ResourceDictionary>
            </StackLayout.Resources>

            <Grid BackgroundColor="{Binding RowColor}" Style="{StaticResource MatchDetailInfoRow}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="42" />
                </Grid.RowDefinitions>

                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Style="{StaticResource MatchInfoMinutes}"
                    Text="{Binding MatchTime}"
                    AutomationId="{Binding TimelineEvent.Id, StringFormat='MatchTime-{0}'}" />

                <Label
                    Grid.Row="0"
                    Grid.Column="1"
                    HorizontalTextAlignment="End"
                    Style="{StaticResource MatchInfoMainPlayer}"
                    Text="{Binding HomePlayerName}"
                    AutomationId="{Binding TimelineEvent.Id, StringFormat='HomePlayerName-{0}'}" />
                <FlexLayout
                    Grid.Row="0"
                    Grid.Column="2"
                    Style="{StaticResource FlexLayoutAlignCenter}">
                    <Image
                        HorizontalOptions="EndAndExpand"
                        IsVisible="{Binding VisibleHomeImage}"
                        Source="{Binding ImageSource}"
                        Style="{StaticResource MatchInfoImage}"
                        AutomationId="{Binding TimelineEvent.Id, StringFormat='HomeImage-{0}'}" />
                </FlexLayout>
                <StackLayout
                    Grid.Row="0"
                    Grid.Column="3"
                    Style="{StaticResource MatchInfoScoreContainer}">
                    <Button
                        IsVisible="{Binding VisibleScore}"
                        Style="{StaticResource MatchInfoScoreBox}"
                        Text="{Binding Score}"
                        AutomationId="{Binding TimelineEvent.Id, StringFormat='Score-{0}'}" />
                </StackLayout>
                <FlexLayout
                    Grid.Row="0"
                    Grid.Column="4"
                    Style="{StaticResource FlexLayoutAlignCenter}">
                    <Image
                        IsVisible="{Binding VisibleAwayImage}"
                        Source="{Binding ImageSource}"
                        Style="{StaticResource MatchInfoImage}"
                        AutomationId="{Binding TimelineEvent.Id, StringFormat='AwayImage-{0}'}" />
                </FlexLayout>

                <Label
                    Grid.Row="0"
                    Grid.Column="5"
                    Style="{StaticResource MatchInfoMainPlayer}"
                    Text="{Binding AwayPlayerName}"
                    AutomationId="{Binding TimelineEvent.Id, StringFormat='AwayPlayerName-{0}'}" />
            </Grid>
            <BoxView Style="{StaticResource HorizontalLine}" />
        </StackLayout>
    </ViewCell>
</DataTemplate>