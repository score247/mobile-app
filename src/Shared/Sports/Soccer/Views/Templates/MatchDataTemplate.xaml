<?xml version="1.0" encoding="UTF-8" ?>
<DataTemplate
    x:Class="LiveScore.Soccer.Views.Templates.MatchDataTemplate"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:coreConverters="clr-namespace:LiveScore.Core.Converters;assembly=LiveScore.Core;"
    xmlns:coreResources="clr-namespace:LiveScore.Core.ViewResources;assembly=LiveScore.Core;">
    <ViewCell>
        <StackLayout>
            <StackLayout.Resources>
                <ResourceDictionary>
                    <coreConverters:MatchStatusConverter x:Key="matchStatusConverter" />
                    <ResourceDictionary.MergedDictionaries>
                        <coreResources:MatchViewResource />
                    </ResourceDictionary.MergedDictionaries>
                </ResourceDictionary>
            </StackLayout.Resources>
            <FlexLayout Style="{StaticResource FlexLayoutHorizontalCenter}">
                <Label
                    IsVisible="{Binding MatchResult.EventStatus.IsClosed}"
                    Style="{StaticResource MatchStatus}"
                    Text="{Binding MatchResult.EventStatus, Converter={StaticResource matchStatusConverter}}" />
                <Label
                    IsVisible="{Binding MatchResult.EventStatus.IsLiveOrNotStarted}"
                    Style="{StaticResource MatchStatus}"
                    Text="{Binding DisplayLocalTime}" />
                <StackLayout Style="{StaticResource TeamContainer}">
                    <Label Style="{StaticResource HomeTeam}" Text="{Binding Teams[0].Name}" />
                    <Label Style="{StaticResource AwayTeam}" Text="{Binding Teams[1].Name}" />
                </StackLayout>
                <StackLayout Style="{StaticResource ScoreContainer}">
                    <Label Style="{StaticResource HomeScore}" Text="{Binding MatchResult.HomeScore}" />
                    <Label Style="{StaticResource AwayScore}" Text="{Binding MatchResult.AwayScore}" />
                </StackLayout>
                <Label AutomationId="MatchFavoriteBtn" Style="{StaticResource MatchFavoriteIcon}" />
            </FlexLayout>
        </StackLayout>
    </ViewCell>
</DataTemplate>
