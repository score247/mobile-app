<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary
    x:Class="LiveScore.Soccer.ViewResources.MatchDetailViewResource"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Style x:Key="NoInfo" TargetType="Label">
        <Setter Property="Margin" Value="16,12" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}" />
        <Setter Property="FontSize" Value="{StaticResource SmallTextFontSize}" />
    </Style>

    <Style x:Key="MatchGeneralInfoContainer" TargetType="StackLayout">
        <Setter Property="Margin" Value="8,8,8,0" />
        <Setter Property="Padding" Value="1,0,1,1" />
        <Setter Property="BackgroundColor" Value="#2D2F3F" />
    </Style>

    <Style x:Key="MatchGeneralInfoLeagueBox" TargetType="Grid">
        <Setter Property="Padding" Value="0,0,12,0" />
    </Style>

    <Style x:Key="MatchGeneralInfoTeams" TargetType="Grid">
        <Setter Property="BackgroundColor" Value="#0B0D14" />
        <Setter Property="Padding" Value="0,0,12,0" />
    </Style>

    <Style
        x:Key="MatchGeneralInfoTeam"
        BasedOn="{StaticResource Team}"
        TargetType="Label">
        <Setter Property="FontAttributes" Value="Bold" />
    </Style>

    <Style x:Key="MatchGeneralInfoImportantContent" TargetType="Label">
        <Setter Property="FontSize" Value="{StaticResource SmallerTextFontSize}" />
        <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}" />
        <Setter Property="BackgroundColor" Value="#2D2F3F" />
        <Setter Property="Margin" Value="8,0" />
        <Setter Property="HeightRequest" Value="23" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
    </Style>

    <Style
        x:Key="Group"
        BasedOn="{StaticResource FlexLayoutHorizontalCenter}"
        TargetType="FlexLayout">
        <Setter Property="BackgroundColor" Value="{StaticResource FourthAccentColor}" />
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="HeightRequest" Value="{StaticResource FunctionGroupHeight}" />
    </Style>
    <Style x:Key="GroupName" TargetType="Label">
        <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="FontSize" Value="{StaticResource SmallerTextFontSize}" />
    </Style>
    <Style x:Key="TopInfo" TargetType="FlexLayout">
        <Setter Property="JustifyContent" Value="SpaceBetween" />
        <Setter Property="Padding" Value="0,0,0,5" />
        <Setter Property="BackgroundColor" Value="{StaticResource PrimaryColor}" />
    </Style>

    <!--  SubTab  -->

    <Style x:Key="BetTypeContainer" TargetType="Grid">
        <Setter Property="BackgroundColor" Value="{StaticResource SubTabContainerBgColor}" />
        <Setter Property="Padding" Value="1,1,1,0" />
        <Setter Property="ColumnSpacing" Value="1" />
    </Style>
    <Style x:Key="SubTab" TargetType="Button">
        <Setter Property="FontSize" Value="{StaticResource SubTabsFontSize}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="TextColor" Value="{StaticResource SubTabColor}" />
    </Style>
    <Style x:Key="SubTabHorizontalLine" TargetType="BoxView">
        <Setter Property="BackgroundColor" Value="{StaticResource ActiveSubTabBgColor}" />
        <Setter Property="HeightRequest" Value="1" />
    </Style>
</ResourceDictionary>