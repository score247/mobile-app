<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:LiveScore.Common.Controls.TabStrip"
             x:Name="this"
             x:Class="LiveScore.Common.Controls.TabStrip.TabStrip"
             VerticalOptions="FillAndExpand"
             HorizontalOptions="FillAndExpand">
    <ContentView.Resources>
        <ResourceDictionary>
            <local:TabViewSelector x:Key="tabViewSelecter">
            </local:TabViewSelector>
        </ResourceDictionary>
    </ContentView.Resources>
    <ContentView.Content>
        <StackLayout>
            <local:TabStripHeader x:Name="TabHeader" BackgroundColor="#0F111C" ItemsSource="{Binding ItemsSource}">
            </local:TabStripHeader>
            <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                <ContentView x:Name="TabContent">
                    <ContentView.GestureRecognizers>
                        <SwipeGestureRecognizer Direction="Left" Swiped="OnSwiped" />
                        <SwipeGestureRecognizer Direction="Right" Swiped="OnSwiped" />
                    </ContentView.GestureRecognizers>
                </ContentView>
            </StackLayout>
        </StackLayout>
    </ContentView.Content>
</ContentView>