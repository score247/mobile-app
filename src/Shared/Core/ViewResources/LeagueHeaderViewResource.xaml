<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary x:Class="LiveScore.Core.ViewResources.LeagueHeaderViewResource"
                    xmlns="http://xamarin.com/schemas/2014/forms"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <Style x:Key="LeagueGroupBox"
           TargetType="StackLayout">
        <Setter Property="BackgroundColor"
                Value="{StaticResource FunctionGroupBgColor}" />
        <Setter Property="HeightRequest"
                Value="30" />
    </Style>
    <Style x:Key="LeagueGroupName"
           TargetType="Label">
        <Setter Property="TextColor"
                Value="{StaticResource PrimaryAccentColor}" />
        <Setter Property="FontSize"
                Value="{StaticResource FunctionGroupFontSize}" />
        <Setter Property="FontFamily"
                Value="{StaticResource RobotoMedium}" />
        <Setter Property="VerticalTextAlignment"
                Value="Center" />
        <Setter Property="VerticalOptions"
                Value="CenterAndExpand" />
        <Setter Property="Padding"
                Value="10,0,10,0" />
    </Style>
    <Style x:Key="RegionBox"
           TargetType="Grid">
        <Setter Property="BackgroundColor"
                Value="{StaticResource FunctionGroupBgColor}" />
        <Setter Property="HeightRequest"
                Value="25" />
    </Style>
    <Style x:Key="RegionName"
           TargetType="Label">
        <Setter Property="TextColor"
                Value="{StaticResource FunctionGroupColor}" />
        <Setter Property="FontSize"
                Value="{StaticResource FunctionGroupFontSize}" />
        <Setter Property="FontFamily"
                Value="{StaticResource RobotoMedium}" />
        <Setter Property="VerticalTextAlignment"
                Value="Center" />
        <Setter Property="VerticalOptions"
                Value="CenterAndExpand" />
        <Setter Property="Padding"
                Value="10,0,10,0" />
    </Style>
    <Style x:Key="LeagueBox"
           TargetType="StackLayout">
        <Setter Property="BackgroundColor"
                Value="{StaticResource SecondColor}" />
        <Setter Property="HeightRequest"
                Value="25" />
    </Style>
    <Style x:Key="LeagueName"
           TargetType="Label">
        <Setter Property="TextColor"
                Value="{StaticResource FunctionGroupColor}" />
        <Setter Property="FontSize"
                Value="{StaticResource FunctionGroupFontSize}" />
        <Setter Property="FontFamily"
                Value="{StaticResource RobotoMedium}" />
        <Setter Property="VerticalTextAlignment"
                Value="Center" />
        <Setter Property="VerticalOptions"
                Value="CenterAndExpand" />
        <Setter Property="Padding"
                Value="10,0,10,0" />
    </Style>
</ResourceDictionary>