<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="LiveScore.News.Views.EmptyNewsView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
    xmlns:mvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    x:Name="self"
    Title="{Binding Title}"
    prism:ViewModelLocator.AutowireViewModel="True"
    BackgroundColor="{StaticResource PrimaryColor}">
    <NavigationPage.TitleView>
        <FlexLayout Style="{StaticResource FlexLayoutVerticalCenter}">
            <Label
                AutomationId="NavigationTitle"
                Style="{StaticResource PageTitle}"
                Text="{Binding Title}" />
            <StackLayout x:Name="SportName">
                <Label Style="{StaticResource SportName}" Text="SOCCER" />
            </StackLayout>
        </FlexLayout>
    </NavigationPage.TitleView>
    <ContentPage.ToolbarItems>
        <ToolbarItem
            Name="Search"
            Icon="images/common/icon_transparent.png"
            Priority="1" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Label Text="News View" TextColor="{StaticResource PrimaryTextColor}" />
    </ContentPage.Content>
</ContentPage>
