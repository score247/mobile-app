<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:ffimageloadingsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
             xmlns:coreViewResources="clr-namespace:LiveScore.Core.ViewResources;assembly=LiveScore.Core"
             xmlns:coreTemplates="clr-namespace:LiveScore.Core.Views.Templates;assembly=LiveScore.Core"
             xmlns:viewResource="clr-namespace:LiveScore.Features.League.ViewResources;assembly=LiveScore.Features"
             xmlns:selectors="clr-namespace:LiveScore.Features.League.Views.Templates.Selectors;assembly=LiveScore.Features"
             xmlns:mvvm="http://prismlibrary.com"
             xmlns:viewModel="clr-namespace:LiveScore.Features.League.ViewModels"
             mc:Ignorable="d"
             mvvm:ViewModelLocator.AutowireViewModel="True"
             x:DataType="viewModel:LeaguesOfCountryViewModel"
             x:Class="LiveScore.Features.League.Views.LeaguesOfCountryView">
    <NavigationPage.TitleView>
        <coreTemplates:NavigationTitleTemplate Title="{Binding Title}"
                                               CurrentSport="{Binding CurrentSportName}" />
    </NavigationPage.TitleView>
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="images/common/icon_transparent.png"
                     Priority="1"
                     Text="Search" />
    </ContentPage.ToolbarItems>
    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <viewResource:LeagueViewResource />
                <coreViewResources:MatchHeaderViewResource />
            </ResourceDictionary.MergedDictionaries>
            <selectors:LeagueItemTemplateSelector x:Key="LeagueItemTemplateSelector" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid CompressedLayout.IsHeadless="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="1" />
                <RowDefinition Height="{StaticResource FunctionGroupHeight}" />
                <RowDefinition Height="1" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <BoxView Style="{StaticResource RegionDetailGroupBox}"
                     Grid.Column="0"
                     Grid.ColumnSpan="2"
                     Grid.RowSpan="2" />
            <BoxView Grid.Row="0"
                     Grid.Column="0"
                     Grid.ColumnSpan="2"
                     Style="{StaticResource HorizontalLine}" />
            <ffimageloadingsvg:SvgCachedImage Grid.Row="1"
                                              Grid.Column="0"
                                              DownsampleToViewSize="True"
                                              LoadingPlaceholder="images/flag_league/default_flag.svg"
                                              Source="{Binding CountryFlag, Mode=TwoWay}"
                                              Style="{StaticResource CountryFlag}" />
            <Label Grid.Row="1"
                   Grid.Column="1"
                   LineBreakMode="TailTruncation"
                   Style="{StaticResource LeagueName}"
                   Text="{ Binding LeagueName }" />
            <BoxView Grid.Row="2"
                     Grid.Column="0"
                     Grid.ColumnSpan="2"
                     Style="{StaticResource HorizontalLine}" />
            <ListView AutomationProperties.IsInAccessibleTree="False"
                      Grid.Row="3"
                      Grid.Column="0"
                      Grid.ColumnSpan="2"
                      HasUnevenRows="true"
                      IsPullToRefreshEnabled="true"
                      IsRefreshing="{Binding IsRefreshing}"
                      ItemTemplate="{StaticResource LeagueItemTemplateSelector}"
                      ItemsSource="{Binding LeaguesItems}"
                      SeparatorColor="Transparent"
                      SeparatorVisibility="None"
                      CachingStrategy="RecycleElementAndDataTemplate"
                      Style="{StaticResource ListViewHasBackground}"
                      RefreshCommand="{Binding RefreshCommand}">
            </ListView>
        </Grid>
    </ContentPage.Content>
</ContentPage>