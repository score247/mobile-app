<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="LiveScore.Features.Menu.Views.SelectLanguageView"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:LiveScore.Features.Menu.ViewModels"
             xmlns:languages="clr-namespace:LiveScore.Common.LangResources;assembly=LiveScore.Common"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:DataType="viewModel:SelectLanguageViewModel"
             BackgroundColor="{StaticResource PrimaryColor}">
    <NavigationPage.TitleView>
        <Label Text="{languages:Translate SelectLanguage}" Style="{StaticResource MenuTitle}" />
    </NavigationPage.TitleView>
    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding DoneCommand}"
                     Order="Primary"
                     Priority="1"
                     Text="Done" />
    </ContentPage.ToolbarItems>
    <StackLayout>
        <Label>This is select language view!</Label>
    </StackLayout>
</ContentPage>